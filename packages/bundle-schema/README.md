# bundle-schema

This package defines the machine-readable schema for job bundles written by the macOS control plane.

## Current Artifacts

- `manifest.schema.json`: JSON Schema for `manifest.json`

## Notes

- The manifest schema is intentionally language-neutral.
- Preview artifacts remain optional.
- The schema reflects the canonical state machine from `docs/architecture/ARCHITECTURE-v2.md`.
