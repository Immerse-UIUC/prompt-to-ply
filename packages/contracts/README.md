# contracts

This package holds machine-readable integration contracts shared across the project.

## Current Artifacts

- `rtx-worker.openapi.yaml`: initial OpenAPI contract for the MVP worker service

## Scope

Contracts in this package should be language-neutral and versioned carefully. The goal is to let the macOS app, worker, tests, and future clients share the same source of truth.
